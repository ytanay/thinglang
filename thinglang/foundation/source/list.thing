thing list

    has `std::vector<Thing>` val

    does append with object item returns list
        `self->val.push_back(item);`
        `return self;`

    does contains with object item returns bool
        `auto res = std::find(self->val.begin(), self->val.end(), item) != self->val.end();`
        if res
            return true
        else
            return false