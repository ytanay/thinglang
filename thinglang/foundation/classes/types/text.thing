thing text

    does + with text other returns text
    does eq with text other returns bool
    does not eq with text other returns bool
    does contains with text substring returns bool
    does length returns number
    does hex returns text
    does * with number count returns text

    as list<number>
    as number

    static does from_list with list<number> lst returns text # TODO: convert to list -> bytes -> text
    static does from_byte with number val returns text