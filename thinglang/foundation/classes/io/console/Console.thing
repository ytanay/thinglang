thing Console

    static does write with text message  #TODO: We probably want this to be object and remove the implicit cast
    static does print with text message
    static does print with text message, list<object> params
    static does read_line returns text